<resources>
    <style name="Theme.Share2Text" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Optional system bar colors; Compose will drive colors otherwise -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
